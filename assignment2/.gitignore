# Python
.venv/
__pycache__/
*.pyc

# Generated feature-store artifacts (regenerated by `feast apply` / preprocess)
feature_repo/data/*.parquet
feature_repo/data/registry.db
feature_repo/data/online_store.db

# MLflow tracking store (regenerated by run_experiments.py).
# The human-readable experiment evidence is committed under artifacts/.
mlruns/

# Redundant raw archive (athletes.csv is the committed source)
athletes.zip

# OS
.DS_Store
